# Secrets — never commit real API keys
.env

# Dependencies
node_modules/

# Persisted vector indexes (Phase 3 disk persistence)
data/

# OS / editor noise
.DS_Store
Thumbs.db

# Personal tool settings
.claude/
